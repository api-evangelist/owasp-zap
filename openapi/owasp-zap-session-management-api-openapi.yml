openapi: 3.2.0
info:
  title: ZAP Session Management API
  description: The HTTP API for controlling and accessing ZAP.
  contact:
    name: ZAP User Group
    url: https://groups.google.com/group/zaproxy-users
    email: zaproxy-users@googlegroups.com
  license:
    name: Apache 2.0
    url: https://www.apache.org/licenses/LICENSE-2.0.html
  version: 2.16.1
servers:
- url: http://zap
  description: The URL while proxying through ZAP.
- url: http://{address}:{port}
  description: The URL of a Local Proxy of ZAP.
  variables:
    address:
      description: The address ZAP is listening on.
      default: 127.0.0.1
    port:
      description: The port ZAP is bound to.
      default: '8080'
security:
- {}
- apiKeyHeader: []
- apiKeyQuery: []
tags:
- name: Session Management
paths:
  /JSON/sessionManagement/action/setSessionManagementMethod/:
    parameters:
    - name: contextId
      in: query
      required: true
      description: ''
      schema:
        type: string
    - name: methodName
      in: query
      required: true
      description: ''
      schema:
        type: string
    - name: methodConfigParams
      in: query
      description: ''
      schema:
        type: string
    get:
      description: Sets the session management method for the context with the given ID.
      operationId: sessionManagementActionSetSessionManagementMethod
      tags:
      - Session Management
      responses:
        default:
          $ref: '#/components/responses/ErrorJson'
      summary: Session management action set session management method
      x-summary-source: derived
  /JSON/sessionManagement/view/getSessionManagementMethod/:
    parameters:
    - name: contextId
      in: query
      required: true
      description: ''
      schema:
        type: string
    get:
      description: Gets the name of the session management method for the context with the given ID.
      operationId: sessionManagementViewGetSessionManagementMethod
      tags:
      - Session Management
      responses:
        default:
          $ref: '#/components/responses/ErrorJson'
      summary: Session management view get session management method
      x-summary-source: derived
  /JSON/sessionManagement/view/getSessionManagementMethodConfigParams/:
    parameters:
    - name: methodName
      in: query
      required: true
      description: ''
      schema:
        type: string
    get:
      description: Gets the configuration parameters for the session management method with the given name.
      operationId: sessionManagementViewGetSessionManagementMethodConfigParams
      tags:
      - Session Management
      responses:
        default:
          $ref: '#/components/responses/ErrorJson'
      summary: Session management view get session management method config params
      x-summary-source: derived
  /JSON/sessionManagement/view/getSupportedSessionManagementMethods/:
    get:
      description: Gets the name of the session management methods.
      operationId: sessionManagementViewGetSupportedSessionManagementMethods
      tags:
      - Session Management
      responses:
        default:
          $ref: '#/components/responses/ErrorJson'
      summary: Session management view get supported session management methods
      x-summary-source: derived
components:
  schemas:
    ErrorJson:
      type: object
      required:
      - code
      - message
      properties:
        code:
          type: string
        message:
          type: string
        detail:
          type: string
  responses:
    ErrorJson:
      description: Error of JSON endpoints.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorJson'
  securitySchemes:
    apiKeyHeader:
      type: apiKey
      name: X-ZAP-API-Key
      in: header
    apiKeyQuery:
      type: apiKey
      name: apikey
      in: query
